Refine the LinkedIn Headline.

## Context to load first
1. Read `{{CONTENT_DIR}}/linkedin/INSTRUCTIONS.md` — voice guidelines, identity context, and file conventions
2. Read `{{CONTENT_DIR}}/linkedin/LinkedIn Current State.md` — what's actually live today
3. Read `{{CONTENT_DIR}}/linkedin/LinkedIn Headline.md` — the current draft
4. Read each note in the `based-on` front matter of `LinkedIn Headline.md`

## Apply the refinement
The user's message after `/li-headline` is the refinement direction. Apply it to the `## Proposed` section only.

LinkedIn gives 220 characters. The headline must stand alone — it appears in search results without surrounding context. Think in terms of stable capability labels, not project names or time-bound language.

## Write the updated file
- Edit only `## Proposed` in `LinkedIn Headline.md`
- Update `last-updated` in front matter to today's date
- Add a row to `## Changelog` with today's date and what changed

Show the revised `## Proposed` in full, followed by 2–3 sentences explaining the key changes.
